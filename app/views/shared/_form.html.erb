  <h2 id="booking-title"> Book your trip in 🛸 </h2>
    <%= simple_form_for [@spaceship, @booking] do |f| %>
      <%= f.input :departure_date, as: :string, required: false, input_html: {class: "datepicker"} %>
      <%= f.input :arrival_date , as: :string, required: false, input_html: {data: {price: spaceship.price}, class: "datepicker"} %>
      <div class="price-div d-none" > Estimated Price : <span id="price"></span>€</div>
      <%= f.button :submit, "Book your Spaceship", id: 'sweet-alert-demo' %>
    <% end %>
    </div>
</div>

